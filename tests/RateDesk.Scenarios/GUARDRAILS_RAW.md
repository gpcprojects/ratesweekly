# Guardrail proposals (raw)

All 212 proposals returned by the 2026-08-31 scenario fleet, verbatim and
undeduplicated. Distilled into eighteen items in `CATALOGUE_101_200.md`.

- [clock] Clamp the pinned gate to the marks' own date: in DailyBuilder.cs:229-231 compute the London now ONCE per run and pass that single value forward, rather than calling DecisionClock.LondonNow() a second time after SnapDiscipline.Apply has already called it at SnapDiscipline.cs:37 — return the resolved `now` from Apply and build MarksAsOfLondon from it, so a run that spans midnight cannot produce a gate dated after its own prices.
- [clock] Assert the invariant in PricingService itself: reject (or log-and-floor) any MarksAsOfLondon set to a future timestamp at the setter in PricingServiceBoards.cs:264-269. A gate later than DecisionClock.LondonNow() is always a bug, never a legitimate state, and the property is the one place both callers pass through.
- [clock] Set MarksAsOfLondon in EmailBuilder.Build the way DailyBuilder does — capture the mode at EmailBuilder.cs:120 instead of discarding it, and set svc.MarksAsOfLondon before EmailBuilder.cs:171 — then add EmailBuilder's LateAnnouncementNotes to rep.Notes so the weekly says why a decided meeting is still on its board.
- [clock] Pass the marks' clock to the dashboards: give RollingStrip.ForMeetings' nowLondon parameter an actual argument at CurrencyPage.cs:48 and MoverScan.cs:307 (threaded from the run's snap mode, like svc.MarksAsOfLondon), so the public pages and the blast cannot disagree about which meetings exist.
- [clock] Replace every DateTime.Today that participates in the London trading calendar with a single London-date helper — the critical sites are HistoryStore.cs:384 and :402 (UpsertDaily/exclusion), StoreBackedHistory.cs:42 and :53 (freshness and gap width), RefDataClient.cs:315 and :390 (BDH end date), and PricingServiceWeekly.cs:286-288 (the d1/w1/m1 anchors). Leave a comment naming the UTC+3 desk box and the UTC+8/+9 VDI case so it is not reverted.
- [clock] Stamp reports on the London clock, not the machine's: PricingServiceWeekly.cs:163 sets rep.AsOf = DateTime.Now, which feeds the blast title (DailyBlast.cs:42-43), the workbook filename (DailyBook.cs:18) and both Outlook subjects (MainWindow.xaml.cs:888, :726). One local-vs-London day shift mislabels the whole day's product.
- [clock] Make the unpinned-mark count a REPORT NOTE, not a log line: SnapDiscipline.cs:76-77 counts tickers that kept a press-time mid and returns (mode, null). Return a CHECK note naming them so ConfirmChecks (MainWindow.xaml.cs:335) gates a board whose rows were marked hours apart — and consider refusing to publish a rung that could not be pinned once the mode is Snap1615.
- [clock] Judge staleness on the mark, not the quote: once SnapDiscipline has pinned a ticker, record the pinned bar's own timestamp alongside the value and have PricingServiceBoards.cs:1073 test THAT age. Otherwise every post-16:15 run flags the whole European board and advises a contributor switch it does not need.
- [clock] Give decisionTimeLondon a source-of-truth shape: store each bank's announcement as a local time plus an IANA/Windows timezone id and convert per decision date, so the FOMC (config/meetings.json:68) and BOC (:241) are automatically 18:00/13:45 London in the US-DST-mismatch weeks (26-31 Oct 2026, 14-27 Mar 2027) and the southern-hemisphere banks (RBA :300, RBNZ :358) follow their own switchovers.
- [clock] Extend CalendarHealth beyond parseability: it currently only tests TimeSpan.TryParse at CalendarHealth.cs:81. Add (a) a warning whenever a bank's converted announcement time for an upcoming decision differs from its configured string, and (b) a warning for banks whose release time is a band rather than a point (BOJ, config/meetings.json:460) so a run at the edge of that band degrades honestly instead of asserting a decision.
- [clock] Refuse, or hard-gate, a run whose London hour makes it meaningless: SnapDiscipline.Resolve (:27-30) maps 00:20 and 09:00 to the same PreClose branch, and MainWindow.xaml.cs:335 exempts the PRE-CLOSE note from the modal. Split the branch — a genuine morning run stays informational; a run before ~06:00 London, or one whose report AsOf is more than a few hours old at CREATE EMAIL time, must block or warn on the surface itself.
- [clock] Give the CLI the GUI's gate: Cli/Program.cs:175-197 (`daily`) writes the blast, workbook and shared-drive copy with no ConfirmChecks and at whatever hour a scheduled task fires. Have it fail non-zero on any CHECK note unless an explicit --force is passed, and refuse to run at all outside a configured London window.
- [lag] Add "fixingLagDays": 1 to the MPC entry (config/meetings.json:147 block) and delete it from SNB (:376) unless the desk confirms SARON publishes same-day; back it with a unit test asserting fixingLagDays == 1 for every run whose MeetingCalendar.DecisionToStartLagDays is 0, and 0 for the rest - that one assertion is the whole class.
- [lag] Make the re-base window's business-day walk (PricingServiceBoards.cs:918-924) skip the FIXING's own publication holidays, not just weekends, or the MPC fix leaves a Good-Friday hole and an FOMC-before-a-holiday hole behind it.
- [lag] Run the existing plausibility test on the re-base BASE before adopting it (PricingServiceBoards.cs:932-935): reject a pending mid more than 25bp from its quoted neighbours, and refuse outright a base drawn from a period TurnAt() marks as a Y/E turn - fall through to step 3 with RefRebasedStale rather than adopt a number the app will not publish as a row.
- [lag] Print the base's provenance, not just a dagger: SheetEmail.cs:183 collapses live-mark, recorded-close and pre-statement-close into one footnote although RefRebasedStale already distinguishes them.
- [lag] Replace the hard 10-day DecisionClock.PairToleranceDays (src/RateDesk.Core/Dates/DecisionClock.cs:16) with a per-run maxEffectiveLagDays, and make a breach a CHECK note on the DAILY surfaces ("announced decision dd-MMM has no pairable period start - Priced is measured against the pre-decision fixing") instead of three silent no-ops.
- [lag] Wire CalendarHealth into the daily report notes (it runs only in the weekly UpdateEngine and lands in calendar_health.txt), and CHECK-prefix checks 1, 2 and 3 so the pre-publish gate sees a drifted grid on the day it matters, not a week later in a side file.
- [lag] Surface MeetingsStore.Origin (set at PricingServiceBoards.cs:229, read nowhere in the product) plus the calendar's newest decision date on every surface's provenance line, and move the override out of publish\config\ to %APPDATA%\RatesWeekly so the release ritual's publish\ swap cannot silently delete it.
- [lag] Tighten the per-horizon anchor slack to what the label can bear - Delta1w's 7 days permits a 14-day change (PricingServiceWeekly.cs:287) - and whenever a cell publishes off a walked-back anchor, name the anchor date in a note.
- [lag] Give RollingStrip.ForMeetings (RollingStrip.cs:223) and MoverScan (MoverScan.cs:310) the recordedEffective and ObservedShifts arms the boards already have, cap RolledValue's walk-back (RollingStrip.cs:153), and add an invariant that a strip level equals the board mid for every contract both surfaces publish.
- [lag] Make MeetingRungMap treat PastDates the way MeetingCalendar does - a hand-entered decision gets the derived lag, and for a rollsAtPeriodStart family it must be projected to the START (MeetingRungMap.cs:47-49 vs MeetingCalendar.cs:44-46); or forbid hand-entered pastDates on such runs and say so at config/meetings.json:8.
- [lag] Stop clustering two ANNOUNCEMENTS together: cluster only an announcement with its own period start (MeetingRungMap.cs:52-56). The "no two real meetings of one bank sit within 14 days" invariant is broken by every inter-meeting move, and 14 is inclusive, so even a genuinely scheduled pair exactly 14 days apart collapses.
- [lag] Fix MeetingCalendar.cs:23-24 to a real median and refuse to derive a lag at all from fewer than three paired meetings, so a trimmed or half-consumed calendar cannot promote the largest observed lag to "the" lag.
- [lag] Either extend FuturesGuard to compare (blend - fixing) against the published Priced, or delete "or a wrong re-base" from its docstring (FuturesGuard.cs:6-9): as written it tells the desk that green means the base is sound.
- [roll] MeetingRungMap.cs:138-158 — refuse the offset calibration outright when the day is a boundary or mixed-state, and require at least two recorded rungs plus a calibrated offset of 0 before it may overrule the step-back at :160. A record stamped at run time cannot speak for a 16:15 snap; DailyBook.cs:161-170 already adopted that rule and the boards contradict it.
- [roll] HistoryStore.cs:195-212 — the maturity table has no observation-TIME column, so no consumer can tell a pre-announcement record from a post-announcement one on the same date. Add the time, and let MeetingRungMap.RungFor use a record only when its observation time is at or before the mark being read.
- [roll] DailyBuilder.cs:279 and EmailBuilder.cs:140 — stamp records with the LONDON date (Dates.DecisionClock.LondonNow().Date), not machine-local DateTime.Today, and refuse to write a record for a date the store has no close for. Every gate in the app already rides the London clock; only the record writes do not.
- [roll] PricingServiceBoards.cs:1562 — give the ObservedShifts ticker lambda the same composite fallback the liveMid lambda at :1564-1567 and `recorded` at :1546-1547 already carry, and never let one series mix spellings: the measured 1.7bp composite/NABZ basis (:1631-1633) is larger than RungShiftScan's 1.5bp FitToleranceBp.
- [roll] RungShiftScan.cs:167 and :192-202 — separate 'the numbering is unknown' from 'the market moved'. Score the shift-0 hypothesis on the residual after removing the strip's common move, and let ShiftSince tolerate an unjudged day where no boundary is plausible instead of returning null for the whole chain. As written, one 2bp day anywhere in the 70-day window disables the only rung derivation that reaches back past 2026-08-26.
- [roll] RungShiftScan.cs:164 — apply MinSupport to shift 0 as well. A Confirmed 'nothing renumbered' on one or two rungs is not evidence, and it is the verdict that silently overrules the calendar.
- [roll] RungShiftScan.cs:105 — `if (day.Count == 0) break;` treats a hole at rung n as the end of the family and discards every rung above it. Skip the hole and keep probing, and report the achieved support so a scan capped below MinSupport is visible.
- [roll] MeetingRungMap.cs:52-56, RollingStrip.cs:62 and MoverScan.cs:164-166 — emit a note naming every candidate date the 14-day cluster dropped, and raise it through CalendarHealth. The collapse is currently silent on every surface, and it is the step that turns a correctly entered extra meeting into a phantom.
- [roll] tests/RateDesk.Tests/BasisGuardTests.cs:613-620 — the invariant the cluster's safety rests on is asserted only over sched.Dates. Extend it to the full candidate set MeetingRungMap actually clusters (Dates ∪ PastDates ∪ MeetingCalendar.AnnouncementDates), and add a runtime assertion that no two kept boundaries derive from different meetings.
- [roll] RollingStrip.cs:223, MoverScan.cs:310, DailyBook.cs:133 and FallbackIngest.cs:74 — all four build MeetingRungMap without recordedEffective while PricingServiceBoards.cs:1551 supplies it. Either pass the same delegate everywhere or make the parameter required, so a record-less map is a decision rather than an omission. Today the investor-facing dashboard is the surface with the least evidence.
- [roll] RollingStrip.cs:246 versus :73 — one rung derivation per asOf for the truncation gate and the published ticker; they differ by exactly one whenever asOf is a boundary, which is every run made the day after a roll.
- [roll] PricingServiceBoards.cs:1627-1641 — FamilyHist must not substitute a later ticker PATTERN's series for a rung of the first family. Apply the refusal ResolveMeetingDates already carries at :669-673, or label the row and blank its changes.
- [roll] PricingServiceBoards.cs:228-229 — surface MeetingsStore.Origin on the blast, the workbook and every dashboard footer, and refuse a file-based override whose newest date predates today. Origin is captured at :217 and read nowhere in src, so a stale config\meetings.json beside the exe replaces the entire boundary set in silence.
- [roll] config/meetings.json — MPC carries no fixingLagDays while the doc at PricingServiceBoards.cs:169-181 states that FOMC and MPC are the exception and both carry 1; the shipped file gives 1 to FOMC and SNB only. If the comment is right, the announced-but-not-yet-effective window (:918-925) closes a day early for MPC and every Priced on the GBP board overstates by the full move on the day after an MPC decision. Settle which is wrong before release.
- [roll] tests/RateDesk.Scenarios/README.md 'Known harness limits' claims CalendarHealth is not merged into the report notes and that production runs it in the weekly UpdateEngine only; DailyBuilder.cs:248 merges it into rep.Notes on the daily cadence. Fix the README before it is used as the coverage map.
- [roll] Every scenario in the catalogue seeds a tape flat to well under 1.5bp/day, which is the only regime in which RungShiftScan can return Confirmed. Add a live-tape variant — one persistent 3bp repricing inside the lookback — to 21, 63 and 64 before the price-based rung derivation is credited as covering the pre-record window.
- [anchors] Give QuoteData.PrevClose a DATE (BDP PX_CLOSE_DT, or fall back to HistoryStore.LastDate for the resolved rung ticker) and make the Δ1d fallback at PricingServiceWeekly.cs:315 refuse an anchor older than its own 5-day cap - today codClean tests boundary and mixed-state only, so a market that was shut for a fortnight publishes the fortnight as Δ1d.
- [anchors] Give RollingStrip.RolledValue the same per-horizon staleness cap the email has: return null when the close it found is more than N days before the requested target (RollingStrip.cs:165 -> HistoryStore.ValueAsOf has no lower bound at all). One shared constant, read by ChangeToBp, RolledValue, DailyBook.Chg and MoverScan.
- [anchors] Add a per-family freshness gate to RollingStrip.ForMeetings: if the family's own newest stored close is more than a few days before the page's asOf (store.LatestDate(), which is the MAX over the whole store), suppress the change cells and stamp the family's real as-of on the panel - today a 19-day-old close renders under a current header.
- [anchors] Change Panels.BpCell (Panels.cs:82) to a three-section format so an exactly flat change reads "0.0", not "+0.0" - the desk rule the email and blast are already tested against (Invariants.cs:256-258) is not enforced on the 28 published dashboards.
- [anchors] Implement the turn stand-down the stitcher's own comment claims (PricingServiceBoards.cs:1773-1774): skip a Y/E-turn rung when choosing loN/hiN, exactly as the live guard does at lines 1027-1028, and never judge a turn contract's own points. Until then the guard is disabled on both rows either side of the Riksbank turn, permanently.
- [anchors] Filter the 16:15 snaps before they overwrite despiked closes (PricingServiceBoards.cs:1654) - the snap path is the one path with no despike and, next to a turn or on the front row, no neighbour guard either. The strip-median arbitration already built for InflHistory.Maintain is the shape to reuse.
- [anchors] Make a missing snap series VISIBLE: PricingServiceBoards.cs:1651 falls back to closes-only when GetLondonSnaps returns empty, and RefDataClient.cs:500-503 returns empty on any exception without caching, so a throttled intraday service silently re-anchors every change column. Emit a note naming the families that anchored on closes.
- [anchors] Make the anchors read the LONDON date, not DateTime.Today: introduce one AsOfLondon on PricingService (fed from DecisionClock.LondonNow().Date) and use it at PricingServiceWeekly.cs:286-288, 308, 431-432, 507-508 and in the WeeklyCurves/RollingStrip asOf path. Then refuse to run, or warn loudly on every surface, when DateTime.Today differs from the London date.
- [anchors] Stamp the anchor DATE the run actually used for each horizon into the frozen report and print it in the run notes - it is the only way a desk can tell a 7-day Δ1w from the 14-day one the cap admits, or spot that a machine ran a day ahead of London.
- [anchors] Make the mixed-state record lift symmetric or explicit: PricingServiceBoards.cs:1704-1706 admits a recorded mixed-state day, DailyBook.cs:169-170 never does. Either align them or have the report name the regime, so the email's Δ1w and its own Historical_ tab cannot disagree and so the number is reproducible on a machine restored from a StoreBackup snapshot.
- [anchors] Cap MoverScan's own anchors (MoverScan.cs:89, 91) - the StaleDays 4 gate at line 79 checks only the newest point, so a 23-day gap becomes a ranked 1w sigma event on the site's landing page, and the page's methodology line at :369 promises otherwise.
- [anchors] Make HistoryFilter.Despike date-aware: its +-window is measured in INDICES, so a hole in the tape turns a 5-print window into a 30-day one and a reopening print is judged against a level a month old. At minimum, skip points whose index neighbours are more than a few calendar days away.
- [anchors] Raise HistoryFilter's MAD floor question with the desk: on a dead-flat rung the effective ceiling is 6*1.4826*0.005 = 4.45bp, and the second arm (dev > 2.5*thr, HistoryFilter.cs:44) rewrites anything over 11.1bp regardless of neighbour agreement - a real 5bp CPI-day round trip is erased before any anchor sees it.
- [anchors] Give the scenario harness an injectable today (and a pinned weekday) rather than reading DateTime.Today - it is the prerequisite for testing the month-end clamp, the local-vs-London date, and the eight weekday-fragile scenarios the 2026-08-31 FINDINGS entry already lists.
- [anchors] Add a Hygiene opt-out flag for scenarios that are deliberately ABOUT the despike filter or the neighbour guard - the current blanket refusal of isolated >4.4bp moves (Market.cs:233, 265-278) makes those two mechanisms untestable by construction.
- [intermeeting] Publish-time contradiction check on the boards: a published row whose own SW_EFF_DT is <= today AND whose mid differs from the o/n fixing by more than 30bp means the policy rate has already changed — raise a blocking CHECK at PricingServiceBoards.cs:1040-1115 rather than printing it as priced-in (threshold above a full step so scenario 58's legitimate un-rolled front is untouched).
- [intermeeting] Promote CalendarHealth check 1 from a CHECK-lane note to a front-table marker: when a live rung's own effective date is absent from config's grid (CalendarHealth.cs:50-52), the mail itself must carry 'calendar behind the feed', because that is the single signature every unrecorded inter-meeting move leaves.
- [intermeeting] Replace the three hard-coded 10-day decision-to-period pairings (DecisionClock.cs:16, CalendarHealth.cs:24, the literal at PricingServiceBoards.cs:925) with one per-run configurable bound seeded from the schedule's own observed LagRange plus a margin, so a long effective date degrades to blank rather than to the stale-fixing arithmetic.
- [intermeeting] Add a per-MEETING decisionTimeLondon override in config/meetings.json: today an off-schedule announcement at an hour the bank's constant does not match leaves the gate and the re-base off for the rest of the day (PricingServiceBoards.cs:859, :895).
- [intermeeting] Make LateAnnouncementNotes (DailyBuilder.cs:340-353) assert only what it can verify — compare the marks' clock against the ANNOUNCEMENT the run is gating on, and never claim 'every price in this run was taken before it' when only the config's default hour says so.
- [intermeeting] Mirror CalendarHealth check 3: it flags an OBSERVED re-point with no calendar boundary (CalendarHealth.cs:91-98); add the inverse — a boundary passed and the front rung's maturity did not move within 2 business days — so a cancelled or postponed meeting is flagged instead of silently rolling the board.
- [intermeeting] Runtime test of the invariant the 14-day cluster rests on: warn loudly at config load (MeetingsStore.Load, PricingServiceBoards.cs:219-247) when two of one bank's boundaries fall within MeetingRungMap.ClusterDays — an inter-meeting move breaks it by definition and the collapse is otherwise invisible.
- [intermeeting] MeetingRungMap.cs:151-157: when the record-calibrated rung comes out below 1 the answer is 'no rung held this contract' — return null instead of falling through to the naive boundary count, which currently resurrects rung 1 against the class's own documented rule at :25-27.
- [intermeeting] RungShiftScan.ShiftSince (RungShiftScan.cs:192-202) must return Unknown, not 0, when there is no judged day-pair covering the interval — a run on a non-business day (Sunday, 15-Mar-2020) or a store whose newest close predates the renumber currently gets a confident zero.
- [intermeeting] Pass the store's recordedEffective delegate into the dashboard strip's MeetingRungMap (RollingStrip.cs:223) so the 28 public pages get the same evidence the email gets, instead of a pure calendar derivation.
- [intermeeting] Pre-upload reconciliation gate: the email's front-row date and level for each bank must equal the dashboard strip's first row (Surfaces.cs:155-161 shows the two derivations side by side) — refuse to publish the site on a mismatch.
- [intermeeting] Set fixingLagDays = 1 for MPC in config/meetings.json — the code's own reasoning names MPC (PricingServiceBoards.cs:179-181) and the flag instead sits on SNB (config/meetings.json:376), which is excluded from the report entirely.
- [intermeeting] Route the front table's Mid, Fixing and Priced cells and the meeting card's Mid cell through NoBrk (WeeklyEmail.cs:165-175, :266): Priced and Step already are, and a move to a negative policy rate is the first time those cells will carry a hyphen for Word to break after.
- [guards] OutlierGuard: add a Priced/Step arm, or say in DESIGN.md that Priced is unguarded. Nothing in the repo inspects PricedBp or StepBp - OutlierGuard.cs:33-34 selects D1Bp/W1Bp/M1Bp only and FuturesGuard.cs:128 reads MidPct - so a wrong fixing or a failed re-base moves the whole headline column and the front table's % of 25bp by a policy step with zero notes. Cheapest concrete gate: flag any run where |front Priced| > 30bp AND the delivered move is not accounted for by a RefRebased flag.
- [guards] RatesSnapshot.BaselineAgeMinutes (RatesSnapshot.cs:107): add an absolute upper bound on top of the relative test. If the 10th-percentile baseline itself exceeds ~90 minutes, or if LAST_UPDATE_DT for the p10 cohort is not today's London date, emit a whole-snapshot note ("the terminal's data plane looks frozen: p10 quote age 450m") rather than letting a uniform freeze cancel itself. Same fix belongs in MainWindow.xaml.cs:427-428, where the SOURCES '*>1h' marker uses Min() of the probed ages.
- [guards] FuturesGuard: make the two silent exits loud. FuturesGuard.cs:48-49 ("run too short") and :90-91 ("no covered, quoted contract window found — skipped") both return non-CHECK text, so MainWindow.xaml.cs:334 filters them out of the gate. A bank whose board IS published but whose independent cross-check could not run should raise a CHECK note naming it - a two-row run can never span two consecutive month boundaries, so truncation disables the guard automatically on exactly the days the data is worst.
- [guards] FuturesGuard ok line (FuturesGuard.cs:79-80): print the guard's resolution alongside the gap. At a 2.5bp tolerance over a 31-day monthavg window, a 25bp step gives 0.806bp per day of boundary error, so the guard is blind to date errors under ~3.1 days (~7.7 days on a 10bp step). The line should say so, or the desk will keep reading "ok" as confirmation the Start/End dates are right.
- [guards] FuturesGuard.MidAt (FuturesGuard.cs:125-130): decide explicitly what a MASKED row contributes. A Y/E-turn or guard-rejected row publishes a LABEL on every surface but its MidPct is still the raw (possibly rejected) print, so the blend is built from a number the board refused to publish - a >25bp rejection guarantees a FUTURES GUARD TRIGGERED whose stated diagnosis ("roll/calendar/re-base fault") is wrong. Skip masked rows, or carry the neighbour-implied level and say which.
- [guards] MainWindow.ConfirmChecks (MainWindow.xaml.cs:332-342): sort by severity, cap, and make it scrollable. FUTURES GUARD TRIGGERED, the completeness gate and rejected-mid notes first; then at most ~8 outlier lines with "and N more — see the log"; dedupe the same bank/horizon across rows. As written, 40+ absolute-bar notes on a repricing day bury the one signal that is almost never anything but a fault, in a Win32 MessageBox that does not scroll.
- [guards] Move the weekly gate ahead of RenderAll, or state that it does not cover the dashboards. MainWindow.xaml.cs:548 writes all 28 currency pages, the movers hub and the single-file pack BEFORE ConfirmChecks at :563, and :592 leaves OPEN OUTPUT enabled on the No path - so declining the gate leaves the investor-facing surface fully rewritten and one click from upload.
- [guards] Run the guards over the dashboard/movers path, or mark it as unguarded in DESIGN.md. RenderAll (MainWindow.xaml.cs:616-661) builds its own PricingService on an empty RatesSnapshot from store data; FuturesGuard.Check, OutlierGuard.Check and the completeness gate all run only inside EmailBuilder.Build / DailyBuilder.Build, so the 28 external client pages and the emailed pack carry no guard coverage of any kind.
- [guards] OutlierGuard cross-sectional arm: guard against a contaminated median. With body.Count == 3 (any 4-row run - MPC, RBNZ and RIKSBANK publish these routinely) a fault in two rows becomes the reference and the guard flags the one honest row. Either require body.Count >= 5 for the median arm, or fall back to a robust scale that cannot collapse to zero, or quote the threshold and MAD in the note so the desk can see the estimator has degenerated.
- [guards] OutlierGuard.Median (OutlierGuard.cs:86-90): document that even-count averaging makes MAD exactly 0.0 whenever 3 of 4 body rows agree, so max(4bp, 4xMAD) degrades to a fixed 4bp trip-wire on every quiet strip. Either raise the floor to the desk's own 5bp visibility bar or add a minimum-dispersion term; as it stands 4.0bp is silent and 4.5bp raises a modal.
- [guards] Neighbour misprint guard (PricingServiceBoards.cs:1027-1036) has a 25bp bar and needs its two neighbours to agree within 25bp, so it cannot see a 10bp level error and switches itself off when two ADJACENT rungs are wrong. Note the second arm at :1786-1787 and RollingStrip.cs:131-134 REPLACES with the neighbour midpoint while this one REJECTS - the divergence should be recorded next to both sites, not just one.
- [guards] CompoundedFixing.Stamp completeness gate (CompoundedFixing.cs:105-116): it asks whether a run EXISTS, not whether it published anything a desk can read. A run whose every row is masked (turn or rejected) passes the gate and prints a block of labels; add a check on the count of rows carrying an actual Mid, and consider raising a note when a bank's published row count drops materially against the previous run's frozen report.
- [inflation] Make CpiFixings.Build read the arbitrated fixings table (store.GetFixingHistory) rather than store.ValueAsOf over raw closes (CpiFixings.cs:87-89) — until it does, the 28 public dashboards publish a mark the email has already rejected.
- [inflation] Add a quote-age gate to the inflation section: run Staleness.Assess (or the >1h rung rule at PricingServiceBoards.cs:1073) over the 36 USSWIF/BPSWIF/EUSWIF quotes in CollectLiveMarks and emit a STALE line per family; today nothing on this path reads q.AgeMinutes.
- [inflation] Remove StaleNotes' `moved < live.Count/2` early return (InflHistory.cs:611) and replace it with an explicit whole-strip-frozen note — a family that did not move at all is the dangerous case, not the safe one.
- [inflation] Suspend close/snap arbitration on any day where MaturityAsOf(tk, day) differs from the previous marked day's, and reset prev[m] (InflHistory.cs:264-275) — mirror the guard CpiFixings.LookBack already has at CpiFixings.cs:127-130.
- [inflation] Key Maintain's rows on EffectiveOn/an exact-day maturity record instead of MaturityAsOf (InflHistory.cs:284) so a day with no record of its own is genuinely skipped, as the class docstring at line 20-23 already promises.
- [inflation] Blank (or footnote) MoM % whenever the previous row's RefMonth is not exactly one month earlier, in DeriveRow/BuildDisplayRows (InflHistory.cs:470, 489-496) and in StoreBooks.WriteInfl's per-day blocks (StoreBooks.cs:305-317).
- [inflation] Print a row per unquoted strip month rather than silently shortening the card — CollectLiveMarks drops it at InflHistory.cs:401 and no surface says a month is missing.
- [inflation] Refuse to publish a Mid whose Base came from hole adoption without a basis stamp: record the basis alongside the adopted print (InflHistory.cs:127-134) and reject a base that disagrees with its neighbours' basis by the HicpRebase ratio.
- [inflation] Bound hole adoption by the index's own ECO_RELEASE_DT rather than the bare 45-day constant (InflHistory.cs:128): 45 days cannot tell "the print is late" from "the print will never exist", and a delayed print makes the app publish YoY off the pricer's forecast base as if it were a print.
- [inflation] Mark or exclude adopted 'xls' index prints where they reach a published cell — Base and YoY carry no provenance today (DeriveRow drops the source PrintsOf knows, InflHistory.cs:449-461), which is a hard-data-rule breach in a client-visible column.
- [inflation] Never let the lone-mover test skip a row for want of a base: `rows[i].BaseV is not { } bse` at InflHistory.cs:569 silently exempts exactly the print-hole rows (Oct-26 CPI) that most need checking — fall back to the family median base.
- [inflation] Fix Maintain's median reference to be what its comment claims: strip.Add(cv - pv) uses prev[m] = the previously CHOSEN mark (line 275), not the previous close, so the discriminator does depend on earlier picks.
- [inflation] Make PinYear refuse rather than guess for months more than four ahead of the observation date (InflHistory.cs:669-672): its comment assumes the front month, but it is applied to every chain break, and on a Sep-2026 save it keys Apr-27..Jul-27 a full year early into 'xls' rows Bloomberg can never overwrite (HistoryStore.cs:120-124).
- [inflation] Write the year into column 2 of the save-down *_History pages (StoreBooks.cs:311-317 writes "MMM" only) so the app's own re-ingest never has to infer it.
- [inflation] Emit a note when an exact change anchor does not exist — a -28d or PrevBd anchor landing on a holiday blanks all 33 Delta1m/Delta1d cells with no explanation (InflHistory.cs:509-511).
- [inflation] Reconcile the two change conventions or label them: the card uses exact -1bd/-7d/-28d while CpiFixings/InflBook use 7-day and same-day-last-month walk-backs (WeeklyCurves.cs:38-39), so the dashboard and the email answer "1m" differently for the same tenor.
- [inflation] Extend the scenario harness to the inflation path — the README puts it out of scope and FINDINGS.md:140 confirms the 64 scenarios never reference InflHistory, so every inflation change ships on four unit tests.
- [compounded] CompoundedFixing.Compound (src/RateDesk.Weekly.Core/CompoundedFixing.cs:44-51): add a precondition that the series contains at least one print dated >= `from`. Today the only staleness test is against `to`, so a window holding zero observations of its own rate regime returns the pre-period rate as a confident, exactly-round number.
- [compounded] CompoundedFixing.cs:133: wrap history.GetDaily(refTicker, lookback) in Analytics.HistoryFilter.Despike, as every other history consumer already does (PricingServiceAnalytics.cs:54, PricingServiceCorr.cs:107, PricingServiceRv.cs:97, PricingServiceBoards.cs:450). The o/n fixing series is the only one in the app read raw, and a bad Friday print weights three calendar days.
- [compounded] SaveDown/StoreBooks.cs:71-76: RefAt walks back to the last fixing on or before each history day with NO staleness cap, so the Historical_* Priced / Step / Percent columns in the shared-drive .xlsm can be based on an arbitrarily old — or junk — fixing print. Cap the walk at CompoundedFixing.MaxStaleDays, despike the series, and blank the cell rather than stamp it.
- [compounded] CompoundedFixing.Stamp (CompoundedFixing.cs:71-98): the XML doc promises a 15bp CHECK note the body does not implement, and the silence order at :85-87 plus the unit test at CompoundedAndSourcesTests.cs:194-199 forbid any note naming compounding. Resolve it explicitly — withhold the value (CompoundedPct = null) above a bar, or emit a bank-named mechanism-free note — and delete whichever claim loses. Right now the number has no gate at all.
- [compounded] CompoundedFixing.cs:129: cross-check the resolved window start against the front rung's own recorded SW_EFF_DT (HistoryStore.cs:65-72 `maturity.effective`, exposed as IHistoryProvider.EffectiveOn) and publish nothing when they disagree. The window start is the last published-adjacent quantity in the app still taken from config alone, which is squarely against the HARD-DATA RULE.
- [compounded] MeetingsStore.Origin (PricingServiceBoards.cs:217, 228-229) is assigned and never read anywhere in src/. Log it on every run and put it on the report notes whenever it is not "embedded" — a hand-edited config\meetings.json beside the exe silently moves every compounded window while the Bloomberg-dated board still looks perfect.
- [compounded] MeetingCalendar.CurrentPeriodStartEx (MeetingCalendar.cs:43-46) promotes ANY hand-entered pastDate to a period boundary at decision + median lag, contradicting the config's own comment that pastDates is 'used only to stitch meeting-ticker HISTORY across rolls' (config/meetings.json:8). Either stop letting pastDates create boundaries for the fixing window, or refuse a derived boundary when MeetingCalendar.LagIsStable (MeetingCalendar.cs:78-90) is false — CompoundedFixing checks only ps.Derived and never the lag's spread.
- [compounded] CompoundedFixing.cs:61: the interior-hole cap is 7 calendar days but the publication lag it is justified by is one business day for every configured index. Split them — keep ~7 for a genuine holiday cluster (BOJ Golden Week runs 6, Norwegian Easter 6) and cap the FIRST segment, `from` to the first real print, at 1-2 days.
- [compounded] CompoundedFixing.cs:139-145: the derived-start guard scans for a fixing change only within +/-4 days of `from` and only when data is present, so it passes both on a phantom boundary six days from the real change and on a series with no points near the boundary. Make an empty or short `near` window a refusal, not a pass.
- [compounded] Daily/DailyBuilder.cs:106-135 (ExportBook) rebuilds from the frozen report and never re-runs CompoundedFixing.Stamp (contrast DailyBuilder.cs:238), so CompoundedPct / CompoundedFrom can describe a period that has already rolled — verified reach: they land in out\daily_report.json and out\weekly_report.json only, NOT in the save-down workbook cells (StoreBooks.cs:87 uses the spot fixing). Either Stamp on the offline path too, or null the pair whenever rep.AsOf is not today.
- [compounded] Add a unit test that pins the FOMC one-pre-effective-EFFR-day convention. CompoundedAndSourcesTests.cs has none: its single reproduction (:28-35) is a FLAT series and pins neither the boundary-day weighting nor fixingLagDays' deliberate absence from the compounded window.
- [store] Introduce a single LondonToday() (DecisionClock already owns the tz) and ban DateTime.Today from every store and change-column call site — HistoryStore.cs:384/402, UpdateEngine.cs:60/93, StoreBackedHistory.cs:42/53, PricingServiceWeekly.cs:286-288, FallbackIngest.cs:107/112, DailyBuilder.cs:279, DailyBuilder.cs:163 — with an analyser or a grep gate in CI; SnapDiscipline.cs:61-66 already proves the fix and is the model.
- [store] At startup, if LondonNow().Date != DateTime.Today, refuse to run and say so (this is the machine-clock analogue of the harness's 00:10-23:50 window) rather than publishing Δ1d = 0.0 silently.
- [store] Make StoreBackup.AfterRun compare before it rotates: refuse to replace history_backup.db when the incoming snapshot has fewer daily rows with source='xls', fewer adopted index prints, or a later MIN(maturity.date) than the one on the share — log '! store backup REFUSED: would lose N irreplaceable rows' and keep both.
- [store] Rotate snapshots by date (history_backup_2026-08-31.db, keep 10) instead of latest/_prev, and make StoreBackup.FindBackup (StoreBackup.cs:57) consider every file in the folder so a crash between the two File.Move calls does not orphan the only good copy.
- [store] Call OfferStoreRestore on every path out of SetupSaveDown, including the 'Locate C+C' and 'Save Locally' branches of AskSaveDownChoice (MainWindow.xaml.cs:242-265), and keep UpdateBtn/DailyBtn IsEnabled="False" in MainWindow.xaml:33/:56 until SetupSaveDown has completed — today WEEKLY RUN creates the db and disqualifies the machine from ever being offered its own history.
- [store] Give StoreBackup a Merge path (import 'xls' rows, adopted prints and maturity records from a snapshot into an existing store, insert-only) so a machine that missed the restore prompt is recoverable; StoreBackup.cs:67's refusal is currently terminal.
- [store] Pass the store's recordedEffective into every MeetingRungMap construction — FallbackIngest.cs:74, DailyBook.cs:133, MoverScan.cs:310, RollingStrip.cs:223 — or make the constructor demand it, and add a unit test asserting all five call sites return the same rung for the same (contract, day) on a day the store has records.
- [store] Have FallbackIngest refuse rather than guess when its own map and the store's recorded SW_EFF_DT disagree about the rung for that day, and surface the refusal as a rep.Notes CHECK, not a log line.
- [store] Never let ExportBook write a file whose name derives from a stale rep.AsOf over the share: either stamp rebuilt books '_rebuilt_<today>' or run IngestNewestSaved before any Sync, and make SaveDownConfig.Sync (SaveDown.cs:118) refuse to overwrite a share file it did not itself write.
- [store] Route InflHistory's '! CHECK: saved book overrides a Bloomberg close' (InflHistory.cs:163) into rep.Notes so ConfirmChecks (MainWindow.xaml.cs:788) gates it — today the desk's only warning that app-derived values have been promoted above Bloomberg scrolls past in a log box.
- [store] Change SetSeededDepth's contract to the depth ACHIEVED: pass min(days, (today − h[0].Date).Days) at UpdateEngine.cs:196, and refuse to raise the watermark when a fetch returns fewer than ~60% of the expected business days.
- [store] Log and note a value CHANGE in UpsertDaily: when ON CONFLICT overwrites a bbg row with a different value, record (ticker, date, old, new) in a revisions table and raise a note when |Δ| exceeds a desk-set bar — HistoryStore.cs:373 currently cannot tell an insert from a rewrite.
- [store] Add a store-depth gate to the daily path mirroring RenderAll's LatestDate check (MainWindow.xaml.cs:620): when TickersCoveringDate(MonthAgo) is below a threshold, or LatestDate is null, emit a CHECK note naming it and keep DAILY EMAIL locked — a blank change column must never be indistinguishable from a flat market.
- [store] Read the runs table: gate publication on the last 'update' row's state, and make RatesWeeklyCli update return a non-zero exit code when Result.Complete is false (Program.cs:44) so a scheduled update→email chain cannot build on a partial seed.
- [store] Rename the single-instance mutex to the Global\ namespace (MainWindow.xaml.cs:112) so the dialog's promise of machine-wide exclusivity is true on RDS/Citrix and across concurrent RDP sessions.
- [store] Validate the CLI's --db (Program.cs:28): reject UNC paths, mapped network drives, any path under a StoreBackup.Folder, and any path inside a OneDrive/synced root, per CLAUDE.md's hard constraint.
- [store] Make StoreBackup.Restore copy -wal/-shm if present (or checkpoint first) and clear the read-only attribute after File.Copy (StoreBackup.cs:69), and refuse a source .db that has WAL siblings — that combination means someone was writing to a snapshot.
- [store] Disambiguate FallbackIngest's date parse (FallbackIngest.cs:164): dd/MM/yyyy is tried before M/d/yyyy on text the doc comment says mixes both, so a US-formatted manual row is filed months away from its true date — require an unambiguous cell or skip the row with a note.
- [savedown] StoreBooks.WriteLivePage must build its BDP tickers from the run's ACTIVE source (svc.MeetingSrc / run.Source), not sched.Source (StoreBooks.cs:160) - and a written-book test must assert every BDP ticker on Runs, Vandit, Blast and Current carries that spelling, because 'SOURCES carries to every surface' has to mean every page of every file.
- [savedown] Stop writing live-price minus frozen-constant into cells headed 'Δ 1d/1w/1m' (StoreBooks.cs:242-249): either write the run's own numbers as static values (matching the Runs sheet cell for cell) or look the anchor up in Historical_* by the contract's START DATE, which is what the docstring at StoreBooks.cs:132-141 still claims. Delete that claim the moment the code stops honouring it.
- [savedown] Any page in a dated workbook that carries live BDP formulas needs a banner in A1 ('LIVE - recalculates; the run's own numbers are on the Runs sheet, as of {asOf}') and every Δ cell should blank itself when TODAY() differs from the report's as-of - one IF per cell, and the -25bp phantom in a week-old book disappears.
- [savedown] ExtractTemplate must never truncate an existing dated book: write to <name>.tmp and File.Replace only after wb.Save() succeeds (StoreBooks.cs:341-350), and refuse outright when the target holds rows dated on/after its own file date that the store does not have.
- [savedown] ExportBook must run the same IngestNewestSaved pass Render runs, before it writes anything (DailyBuilder.cs:103-136 vs 399-413). EXPORT XLS is the button pressed during an outage - i.e. immediately after a hand entry.
- [savedown] FallbackIngest must NOTE every row it drops for minDate or for being dated today (FallbackIngest.cs:107-108), with a count and the file name, exactly as it notes boundary-day skips at 118-124. A silent `continue` on desk-entered data is the one thing this loop must never do.
- [savedown] Give the desk a supported way to enter BACK-DATED outage days - a 'manual' marker column the ingest trusts regardless of the file's date, or a separate entry table - and get the desk's sign-off on which. Today the natural catch-up (type the missing days into today's book) is discarded without a word.
- [savedown] Scrub or rewrite the incumbent Current page on extract: clear CurrentDate (Current!D7/D8/.../D23, currently 46253 = 19-Aug-26) and the change literals in L:N and AB:AD (Current!L7 = -0.10, N7 = -5.52). Same for the unwritten Historical_SZ and 'Coremont Riskbank' tabs. Add a template-freshness assertion so a recapture is forced rather than remembered.
- [savedown] Stamp every written book with machine name, run timestamp and report as-of in a RunInfo cell, and make SyncDailyDir/Sync refuse to overwrite a destination whose RunInfo names a different machine or a newer as-of (DailyBuilder.cs:78-80, SaveDown.cs:118-120). The mtime '>=' comparison also fails silently under share-clock skew and then logs 'shared drive already up to date'.
- [savedown] A save-down leg that throws must add a CHECK note to the report and a ⚠ to the status line, and the status must not print '(+ shared drive)' unless every share write of that run succeeded (DailyBuilder.cs:392, 415-426; MainWindow.xaml.cs:801-803). Give WriteOis, WriteInfl and the two Syncs separate try blocks so one lock does not cancel the backlog mirror.
- [savedown] Pre-flight the destination at the same gate as the outlier notes (MainWindow.xaml.cs:788): path length (root + 'OIS Run History\OIS_Runs_dMMMMyy.xlsm' <= 259), free space, write permission, and whether today's book or the .xlsx attachment is locked by Excel - fail loudly before the blast exists, not quietly after.
- [savedown] StoreBackup must snapshot to a per-machine name and refuse to replace a snapshot holding 'xls' rows, maturity records or an earlier start date than the local store (StoreBackup.cs:36-45); the restore prompt must show total rows, 'xls' rows and date span, not just File.GetLastWriteTime (StoreBackup.cs:53-61, MainWindow.xaml.cs:193-200).
- [savedown] Refuse (or at minimum warn about) a save-down root under OneDrive/Dropbox or a Known-Folder-Move Documents folder (SaveDown.cs:37-38), and make IngestNewestSaved report undatable files with a count and a ⚠ - sync-conflict names ('...-LAPTOP7.xlsm', ' - Copy', ' (1)') currently vanish behind one line (DailyBuilder.cs:158-165).
- [savedown] Do at runtime what Invariants.cs does in the harness: after writing, re-read the book's Runs sheet and compare it against the blast and the report before the email attaches it (DailyBook.cs:37-110). DailyEmail_Click attaches by exact dated name (MainWindow.xaml.cs:914-921), so a failed write leaves a same-named older file that will be attached without complaint.
- [savedown] Move the Outlook COM call off the UI thread and give it a timeout (MainWindow.xaml.cs:882-931): Activator.CreateInstance + mail.Display() run synchronously, so a modal dialog inside Outlook freezes the app with no cancel - the same class of hang the desk has already hit with Excel COM.
- [email] Route every PUBLISHED date through a London calendar, not the machine's: add DecisionClock.LondonToday() beside LondonNow (src/RateDesk.Core/Dates/DecisionClock.cs:21) and use it for WeeklyReport.AsOf (PricingServiceWeekly.cs:163), the three change anchors and prevBd (PricingServiceWeekly.cs:286-288, 308) and every store.SetMaturity(tk, DateTime.Today, …) (DailyBuilder.cs:280, EmailBuilder.cs:140) — ~75 DateTime.Today sites, and SnapDiscipline/MarksAsOfLondon are already London-correct, so the surfaces are the half left behind.
- [email] Put the report's build TIME on every surface, not just its date: DailyBlast's "London EOD OIS Run" headline (Daily/DailyBlast.cs:42), RunsTable.Title (Daily/RunsTable.cs:65) and both mail.Subject lines (MainWindow.xaml.cs:727, 888) should render "16:15 close" or "09:36 PRE-CLOSE" off rep.AsOf's time component, which is already carried and currently discarded by every format string.
- [email] Make an off-list bank LOUD instead of silent: have DailyBlast.Render (Daily/DailyBlast.cs:45) and RunsTable.Build (Daily/RunsTable.cs:74) add a CHECK note when a report run has no DailyBlast.Blocks entry, and have SheetEmail.FrontTable (SheetEmail.cs:152) refuse to publish a front line for a bank that has no run block on the same surface.
- [email] Date-stamp the frozen email fragments and check the stamp at click time: write the as-of into daily_infl_sheet.html / weekly_infl_sheet.html (Infl/InflEmail.cs:46-48) and make InflFragment (MainWindow.xaml.cs:703) refuse a fragment whose as-of does not match the report being composed — the identical rule InflHistory.LoadPersistedMarks already enforces for marks (Infl/InflHistory.cs:381).
- [email] Date the dashboards pack: give SiteFile.FileName an asOf parameter (Render/SiteFile.cs:16) the way DailyBook.FileName has one (Daily/DailyBook.cs:18), and make CreateEmail_Click attach by exact name and say "NO PACK — rebuilt failed" rather than "Dashboards pack attached" (MainWindow.xaml.cs:731-736); a pack failure in RenderAll (MainWindow.xaml.cs:659) must also downgrade the run from WEEKLY COMPLETE.
- [email] Call BodylessWarning() from LoadEmailSettings (MainWindow.xaml.cs:55) and from the top of both CreateEmail_Click (:709) and DailyEmail_Click (:856), not only from Settings_Changed (:76) and CopyEmail_Click (:765) — a session that starts in the no-body-style state is currently silent until someone touches a tickbox.
- [email] Carry RefRebasedStale into WeeklyFront (PricingServiceWeekly.cs:239) and print WeeklyRun.RebasedLabel in the front-table fixing cell and footnote (SheetEmail.cs:165/183, WeeklyEmail.cs:174/185); suppress or flag % 25bp whenever the base is pre-statement, since that column divides by a base the code knows cannot contain the surprise.
- [email] Give the weekly a BCC path or block the button: apply Recipients.Bcc in CreateEmail_Click (MainWindow.xaml.cs:729-733) as the daily does (:906-907), and stop telling the operator to "add recipients and send" (:735) on the surface that carries client-facing links and the dashboards pack.
- [email] Separate "no recipient list" from "unreadable recipient list" in Recipients.Load (Recipients.cs:30-41) and refuse to compose on the latter — the same discipline DailyEmail_Click already applies to an unreadable daily_report.json (MainWindow.xaml.cs:874-880); have Recipients.Save keep a .bak of the previous list (Recipients.cs:43-48) and show a before/after count in the dialog (MainWindow.xaml.cs:290-297).
- [email] Declare the encoding in the clipboard wrapper: add <meta http-equiv="Content-Type" content="text/html; charset=utf-8"> inside `pre` in ClipboardHtml.Set (EmailClipboard.cs:14) — the fragment already carries U+2011 in every date (SheetEmail.cs:75/219), † , Δ and € (Daily/DailyBlast.cs:115), and only the preview file declares a charset (EmailBuilder.cs:238).
- [email] Apply NoBrk to the card email's front-table Priced and % cells (WeeklyEmail.cs:161, 175) — the helper is declared inside the meeting-card loop (:234) so the headline table it protects everywhere else cannot reach it; and make Render.Norm's U+2011 folding (tests/RateDesk.Scenarios/Harness/Render.cs:13-18) an explicit assertion rather than a silent normalisation, so a missing non-breaking hyphen becomes visible to the suite.
- [email] Cap and summarise the CHECK gate (MainWindow.xaml.cs:333-342) and ShowStaleNotes (:348-357): a crisis week where the strip moves 55bp in a month fires OutlierGuard's absolute Δ1m bar on every row of every bank (OutlierGuard.cs:23, 41-45) — roughly 40 notes joined into one unscrollable MessageBox, which trains the desk to click Yes on the gate that matters.
- [sources] StoreBooks.WriteLivePage must build every BDP ticker from run.Source (the frozen report's active contributor), not sched.Source — StoreBooks.cs:160; and WriteOis should assert, per bank, that the live page's ticker string equals the spelling DailyBook.BankHistoryRows used for the same bank (DailyBook.cs:128) before the file is written. One workbook, one feed.
- [sources] SourceStore.Apply must validate what it loads: reject a mnemonic that is not in SourceCatalog.Candidates (SourceCatalog.cs:7-13) and raise a CHECK-prefixed report note — not just a log line — for a sources.json key that matches no schedule, which today is silently dropped at SourceStore.cs:68-70.
- [sources] Enforce the desk ruling in code, not in a JSON comment: BOJ and NORGES must be non-selectable in the SOURCES dialog (extend the exclusion beside SNB at MainWindow.xaml.cs:374-377) and any override for them in sources.json must be ignored with a loud note.
- [sources] Every run whose change anchors fell back to the composite must emit a CHECK-prefixed note naming the bank, the rungs and both spellings — the fallbacks at PricingServiceBoards.cs:1636 and RollingStrip.cs:190-197 are silent today, and one run's worth of that note would tell the desk whether contributor BDH history exists at all.
- [sources] Render the active contributor on the surfaces the desk reconciles: run.Source already rides the frozen report (CompoundedFixing.cs:84) and nothing displays it — put it in the Runs-sheet run header and the blast's run line so a mid-week feed change is visible rather than inferred.
- [sources] Make the source fallback PER DAY, not per series: FamilyHist (PricingServiceBoards.cs:1636) and DailyBook.RungHist (DailyBook.cs:145-152) must merge the two spellings day by day, preferring the active source where it has a mark, so one 'xls' row can never displace a complete composite series.
- [sources] Pin the spelling per CONTRACT across a lookback window in RollingStrip.SourceAwareTicker (RollingStrip.cs:185-198) so no change column can difference two spellings across a renumber boundary; the same rule belongs in MoverScan.MeetingSeries (MoverScan.cs:312).
- [sources] Carry the source selection with the data, not with the machine: stamp the active sources into the store (or into the StoreBackup snapshot) and, on any run where sources.json is absent while the store shows recent non-default spellings, warn before pricing — sources.json lives in %APPDATA% (MainWindow.xaml.cs:15-16) and does not travel.
- [sources] Label the row when the merge took its PRICE from a spelling other than the run's active source: set MidSource accordingly at PricingServiceBoards.cs:1084 instead of a bare "ticker", so the renderers can mark the cell the way they mark a guard-rejected print.
- [sources] Measure staleness on the spelling the desk PICKED: the merged quote carries the price side's AgeMinutes (PricingServiceBoards.cs:684), so a dark contributor rung inherits the composite's freshness and the >1h STALE watch (line 1073) cannot see it.
- [sources] FallbackIngest must file a manual row under the source that was active ON THE ROW'S OWN DATE, not the source active on the run that ingests it — DailyBuilder.cs:365-372 passes today's SrcOf to rows the desk stored days earlier (FallbackIngest.cs:78, 127).
- [sources] The SOURCES dialog's '*>1h' age flag is calibrated to the freshest probed contributor (MainWindow.xaml.cs:425-429), so if every contributor is stale — a holiday, a weekend, a dead feed — none is marked; add an absolute check against the wall clock before the desk trusts a green list.
- [dash] Give HistoryStore.ValueAsOf a mandatory maxStaleDays argument (no default) and port PricingServiceWeekly.ChangeToBp's 10-day rule to every dashboard caller — WeeklyCurves.cs:144, ForwardLadder.cs:116, RollingStrip.cs:165, FraRun.cs:58, Inflation.cs:71/:122 — so a stretched anchor blanks on the pages exactly as it does in the email.
- [dash] Delete the replacement arm of RollingStrip.Guard (RollingStrip.cs:127-137): return null, not the midpoint, and let the row publish MaskLabels.Rejected the way PricingServiceBoards.cs:1120 does. One guard policy, one MaskLabels constant, both surfaces.
- [dash] Add a `Notes` parameter to Panels.Linked and render it as a footnote row: today ForwardLadder's 'points derived from the par ladder', WeeklyCurves' 'N of M pillars had no stored close' and RollingStrip's guard/roll notes are all written and all discarded before the page. A '*' with no legend is not disclosure.
- [dash] Stamp the as-of into the page heading in CurrencyPage.Build and MoversPage.Build, the way SiteFile.cs:39 already does for the attachment. An undated public page cannot be caught being stale by anyone, inside the desk or outside it.
- [dash] Measure MoverScan's freshness gate against DateTime.Today, not against the asOf it is handed (MoverScan.cs:79). As written a uniformly stale store is uniformly fresh, which is the one case the gate exists for.
- [dash] Blank the z column and rank on raw bp until sigmaWeekly is non-null, as DESIGN.md:53-54 specifies — or get the desk to re-sign DESIGN 0a for the sqrt(5) estimate. If the estimate stays, refuse to print a |z| when the vol ratio says the week was normal (|z| > 4 with volRatio < 1.3 is a trend, not a shock).
- [dash] Cap the anchor age inside MoverScan.Stats too: require At(asOf - WeekDays) to resolve within a few days of its target, and require the LEVEL published as LevelNow to be within the same tolerance of asOf — otherwise the row is excluded, not published on a pre-statement mark.
- [dash] Pass the store's maturity record into the dashboard and movers MeetingRungMaps (RollingStrip.cs:223, MoverScan.cs:310) as PricingServiceBoards.cs:1551 does, plus the run's own ticker dates. Evidence before inference on every surface, not just the email.
- [dash] Make the hub refuse to rank a currency whose page this run did not write: have RenderAll collect the set of successfully written ccys and pass it to MoverScan/MoversPage, so a failed page drops out of the ranking and the nav instead of being linked stale (MainWindow.xaml.cs:644-651, Page.cs:42-50).
- [dash] Fail the WEEKLY click when any per-currency render throws or when the run wrote zero rows: promote UpdateEngine's warnings from Log to the ConfirmChecks modal and keep OPEN OUTPUT locked, matching the rule already applied to the email buttons (MainWindow.xaml.cs:592).
- [dash] Extend the CHECK gate to the dashboards. ConfirmChecks' own doc comment admits 'dashboards from the build phase have already run' — reorder RenderAll to run after the gate, or write the pages to a staging directory the gate promotes.
- [dash] Make the CLI's single-currency render refuse a --out directory that already holds an index.html, or force it to rebuild the hub and the pack (Program.cs:86-88). A one-currency refresh into the live site guarantees the hub outranks its pages.
- [dash] Route SnapDiscipline over the forward-grid pillars as well, or make the grid's own header say 'live at press, HH:mm London'. Either way stop pinning the five WeeklyCellOverrides cells inside a live-at-press table — two clocks in one row is the worst of both.
- [dash] Resolve the dashboard forward ladder through the same override table the email uses (PricingServiceWeekly.cs:352), or drop NOK/DKK/CZK from the ladder panel. '1y1y' must not name S0312FS on one surface and S0313FS on the other.
- [dash] Add an assertion to tests/RateDesk.Scenarios/Harness/Invariants.cs Dashboards(): no strip row may publish a level the guard synthesized, and every published strip level must agree with the email's mid for that contract to within a tolerance the desk sets — the current check only compares the FRONT meeting's date and finiteness.
- [dash] Wire MoverScan.Scan and CurrencyPage.Body into Surfaces and add arbitrary-ticker history seeding to Spec (a `Raw(ticker, from, to, value)` alongside Close/Snap/Level). Four of the eight scenarios above are blocked only on that, and the movers hub is the most-read page on the site with zero test coverage.
- [operator] PricingServiceWeekly.cs:163 — set `AsOf = DecisionClock.LondonNow()` and derive the D1/W1/M1 anchor targets at :286-288 (and HistoryStore's excludeToday guard at :384) from that London date, never from `DateTime.Now`/`DateTime.Today`.
- [operator] MainWindow ctor, after :124 — print "exe v0.17.0 · calendar <MeetingsStore.Origin> · configs <ConfigStore.Origin> · machine tz <TimeZoneInfo.Local.Id> · London date <LondonNow().Date>" on the status line, and refuse to run when the tz is not GMT Standard Time until the desk confirms once; today Origin is set at PricingServiceBoards.cs:217 and read nowhere.
- [operator] StoreBackup.AfterRun (StoreBackup.cs:24-45) — refuse to replace `history_backup.db` when the local store holds fewer 'xls'/manual/maturity rows (or >20% fewer daily rows) than the snapshot it would overwrite; log the refusal instead of rotating the good copy out.
- [operator] MainWindow.xaml.cs:197 — offer the store restore whenever the shared snapshot is materially newer or larger than the local store, not only when `File.Exists(DbPath)` is false, and say in the prompt that declining will let the next run overwrite the share.
- [operator] EmailBuilder.cs, immediately after :123 — set `svc.MarksAsOfLondon` exactly as DailyBuilder.cs:229-231 does, and append `DailyBuilder.LateAnnouncementNotes(svc)` to the weekly report so the weekly cannot roll a board past a statement its 16:15 prices predate.
- [operator] MainWindow.xaml.cs:337-341 — use the MessageBox.Show overload with `MessageBoxResult.No` as defaultResult so Enter/Space aborts the CHECK gate instead of publishing.
- [operator] SnapDiscipline PreClose (SnapDiscipline.cs:41) — make the mode CONTENT, not a note: prefix `RunsTable.Title`, the DailyBlast header at DailyBlast.cs:42-43 and the Outlook subject at MainWindow.xaml.cs:888 with "PRE-CLOSE (live mids HH:mm)" whenever the run was not pinned.
- [operator] MainWindow.xaml.cs:799-800 and :838 — re-lock COPY BLAST / DAILY EMAIL when `rep.AsOf.Date != LondonNow().Date` or the report is more than ~2h old, and print the report's full DATE (not just `HH:mm`) in the CopyBlast status line.
- [operator] Program.cs:186 — have the CLI `daily` and `savedown` cases print `rep.Notes`, and exit non-zero having written nothing when any note starts with `OutlierGuard.Prefix`, unless an explicit `--accept-checks` flag is passed; `savedown` should also refuse a report whose AsOf is not today.
- [operator] PricingServiceWeekly.cs:218 — prefix the zero-rows note with `OutlierGuard.Prefix` so a bank that publishes nothing reaches the blocking popup at MainWindow.xaml.cs:334-336.
- [operator] DailyBlast.cs:42 — build the header flag list from the blocks that actually rendered, so a nine-flag header can never sit above an eight-bank table.
- [operator] CalendarHealth.cs:50-52 and :64-67 — prefix the grid-drift and decision-runway warnings with `OutlierGuard.Prefix` so a stale or side-loaded `meetings.json` blocks distribution instead of scrolling past as a log line.
- [operator] MainWindow.xaml.cs:365 — add `if (Busy) return;` to `Sources_Click`, disable `SourcesBtn` for the duration of a run, and snapshot the overrides ONCE per run, passing the same dictionary to RenderAll (:630) and EmailBuilder (EmailBuilder.cs:89) instead of each re-loading sources.json.
- [operator] MainWindow.xaml.cs:410-412 — label every unprobed entry "(unverified)" and block Save on a contributor the live probe did not return for that family; with no terminal, allow only the config default.
- [operator] MainWindow.xaml.cs:548 vs :564 — move `RenderAll` (and the single-file pack) to AFTER `ConfirmChecks`, or re-render on decline, so the dashboards and the attachable pack are not left carrying numbers the desk just refused to publish while OPEN OUTPUT unlocks at :592.
- [operator] DailyBuilder.Build (:193) — call `FallbackIngest.Run` at the START of Build, not only in Render (:370), so a manual outage row typed this morning reaches this run's change anchors rather than the next one's.
- [operator] ConfigStore.LoadDefault (:62-79) — refuse a side-loaded `config\currencies` folder that holds fewer currencies than the embedded set unless explicitly confirmed, and surface the resulting count on the status line.
- [operator] MainWindow.xaml.cs:112 — widen the single-instance mutex from `Local\` to `Global\` and add a lock file under the save-down root, so two Windows sessions on one terminal server (or two machines sharing the C+C root) cannot race `history_backup.db` and the OIS Runs folder.
- [config] Run the config invariants at LOAD time, not only as unit tests: lift the four assertions in tests/RateDesk.Tests/BasisGuardTests.cs:600-639 into a validator called at the end of MeetingsStore.Load (PricingServiceBoards.cs:247) and emit one `CHECK:` per violation. Today every invariant is tested against the EMBEDDED file only, and the documented exe-adjacent override (PricingServiceBoards.cs:228) bypasses all of them.
- [config] Add the missing direction of the pairing invariant, in both the test and the load-time validator: every period start within the next 400 days must have a decisionDate at or before it within 14 days. tests/RateDesk.Tests/BasisGuardTests.cs:623 walks decisions→starts only, which is exactly why ECB 2027-05-06 and NORGES 2027-05-06 pass while both disable the Priced re-base.
- [config] CHECK-prefix the two CalendarHealth warnings that carry the real fault classes — the grid-drift/phantom-meeting line (CalendarHealth.cs:51) and the 90-day runway line (:65) — so they reach MainWindow.ConfirmChecks (MainWindow.xaml.cs:334). Only the two decisionTimeLondon warnings (:78, :82) carry the prefix today, so the zero-touch tripwire cannot stop a press.
- [config] Surface MeetingsStore.Origin and ConfigStore.Origin. Both are set (PricingServiceBoards.cs:229, ConfigStore.cs:35) and read nowhere in the tree — `grep -rn "\.Origin\b" src/` returns nothing — so an override calendar or a stale exe-adjacent config\currencies folder is completely invisible. Print both, with the file mtime and the run count, in the WPF status line and into the frozen report.
- [config] Re-read config\meetings.json on every UPDATE instead of once per process (PricingServiceBoards.cs:214 is a Lazy with no reload), or add MeetingsStore.Reload() and call it at the top of the build. A calendar top-up that does not take effect until relaunch trains the desk to ignore the warning it was acting on.
- [config] Fall back to the embedded calendar when an exe-adjacent override fails to parse, and name the file loudly. MeetingsStore.Load sets Origin before deserializing (:229) and has no fallback (:230-237), so one bad date string in a hand edit throws inside a Lazy that caches the exception for the process lifetime. Keep ReadCommentHandling.Skip and AllowTrailingCommas (:224-225) — the shipped file relies on //-comments — and lock both with a test.
- [config] Pin the three behavioural flags to their owning run: a unit test asserting markTurnPeriods, trustConfigDates and rollsAtPeriodStart are true for exactly {RIKSBANK} across MeetingsStore.Schedules, plus a load-time warning when any of them is set on another run. No test in tests/ asserts this today.
- [config] Make the completeness gate's roster hardcoded rather than config-derived: iterate DailyBlast.Blocks (DailyBlast.cs:17) and raise CHECK for any of the nine banks missing from MeetingsStore.Schedules. CompoundedFixing.cs:103 currently takes its roster from the config it is meant to police, and its second arm (:112) only tests config→Blocks, never Blocks→config.
- [config] Count live futures guards and raise CHECK on an outage: when a run with a non-empty guardFutures returns the "no covered, quoted contract window found" string (FuturesGuard.cs:90) on consecutive runs, that is a dead root or a lapsed entitlement, not a quiet day — and it currently reads identically to the legitimate short-run case and carries no prefix.
- [config] Corroborate trustConfigDates rows before publishing them (PricingServiceBoards.cs:773): reject a config-filled start that sits more than ~7 days from the median inter-meeting spacing measured off the previous resolved row. SEK is the only run allowed to publish config-dated rows and the only run with no guardFutures, so a hand-curated date error there has no second opinion anywhere in the app.
- [config] Widen CalendarHealth check 1 so it can see the rungs that matter: it returns at CalendarHealth.cs:48 whenever a rung publishes no SW_EFF_DT, which is precisely the price-only far rungs trustConfigDates publishes, and its ±10-day PairToleranceDays (:24) would wave through the BOJ-style 8-11-day grid drift documented at MeetingRungMap.cs:13 even where a field exists. Tighten the tolerance for grid-vs-ticker pairing and warn separately when a published row has no field to compare against.
- [config] Warn when MeetingCalendar.AnnouncementDates DERIVES a boundary (MeetingCalendar.cs:100-104) for a period start the front table refuses to date. Today one module invents an announcement (and RollCorrectionDue at PricingServiceBoards.cs:1168 acts on it) while PricingServiceWeekly.cs:235 and DecisionClock.DecisionFor both decline to, so the app is simultaneously confident and honest about the same missing date.
